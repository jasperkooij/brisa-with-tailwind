declare function compile(css: string, { base, onDependency }: {
    base: string;
    onDependency: (path: string) => void;
}): Promise<{
    globs: {
        base: string;
        pattern: string;
    }[];
    build(candidates: string[]): string;
}>;

declare function normalizePath(originalPath: string): string;

export { compile, normalizePath };
